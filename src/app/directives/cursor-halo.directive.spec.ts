import { CursorHaloDirective } from './cursor-halo.directive';

describe('CursorHaloDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorHaloDirective();
    expect(directive).toBeTruthy();
  });
});
